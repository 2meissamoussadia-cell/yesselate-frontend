/**
 * Route /maitre-ouvrage/calendrier/agenda — Vue mois BMO (FullCalendar).
 * Le layout Calendrier parse l'URL et affiche BMOCalendar via CalendrierContentRouter.
 * Cette page ne rend rien : le layout gère tout.
 */
export default function CalendrierAgendaPage() {
  return null;
}
