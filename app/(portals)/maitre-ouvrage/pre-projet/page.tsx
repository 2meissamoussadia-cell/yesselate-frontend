/**
 * Pré-projet — Redirection vers la vue Outlook-like unifiée
 */

import { redirect } from 'next/navigation';

export default function PreProjetPage() {
  redirect('/maitre-ouvrage/pre-projet/outlook');
}
