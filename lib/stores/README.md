# lib/stores — Legacy / doublon

**Les stores utilisés par l’application sont dans `src/lib/stores/`** (imports via `@/lib/stores/*`).

Ce dossier `lib/stores/` est un doublon legacy. Ne pas y ajouter de nouveaux stores ; utiliser `src/lib/stores/` et l’alias `@/lib/stores/`.
