'use client';

/**
 * CommandBar BMO — Réexport du CommandBar unifié (ui/CommandBar).
 * Accepte items (icon ReactNode, primary) ou actions (icon ComponentType).
 */

export {
  CommandBar,
  type CommandBarAction,
  type CommandBarItem,
  type CommandBarProps,
} from '@/components/ui/CommandBar';
